import SignInSection from "@/sections/sign-in/SignInSection";
import React, { FC } from "react";

const SignInPage: FC = () => {
  return (
    <main className="size-fit">
      <SignInSection className="h-screen w-screen" />
    </main>
  );
};

export default SignInPage;
